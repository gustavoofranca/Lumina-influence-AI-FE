{
  "name": "lumina-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Suite de testes ponta a ponta da interface do Lumina.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
